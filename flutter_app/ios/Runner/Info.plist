<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  iOS Info.plist for HUB 2.0
  Key additions for video streaming:
    - NSAllowsArbitraryLoads (dev only) — remove when HTTPS is enforced
    - Privacy usage descriptions for photo library and microphone
    - UIBackgroundModes for audio playback
-->
<plist version="1.0">
<dict>
    <!-- ── App metadata ─────────────────────────────────────────────── -->
    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>
    <key>CFBundleDisplayName</key>
    <string>HUB 2.0</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>hub_streaming</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>$(FLUTTER_BUILD_NAME)</string>
    <key>CFBundleSignature</key>
    <string>????</string>
    <key>CFBundleVersion</key>
    <string>$(FLUTTER_BUILD_NUMBER)</string>

    <!-- ── Supported orientations (all — gyroscope controls programmatically) -->
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>
    <key>UISupportedInterfaceOrientations~ipad</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationPortraitUpsideDown</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

    <!-- ── Background modes (audio continues when app is backgrounded) ─ -->
    <key>UIBackgroundModes</key>
    <array>
        <string>audio</string>
        <string>fetch</string>
    </array>

    <!-- ── Privacy usage descriptions (required by App Store) ─────── -->
    <key>NSPhotoLibraryUsageDescription</key>
    <string>HUB needs access to your photo library to let you select videos for upload.</string>
    <key>NSPhotoLibraryAddUsageDescription</key>
    <string>HUB needs to save downloaded videos to your photo library.</string>
    <key>NSMicrophoneUsageDescription</key>
    <string>HUB uses the microphone if you record a video directly in the app.</string>
    <key>NSCameraUsageDescription</key>
    <string>HUB uses the camera if you record a video directly in the app.</string>
    <key>NSMotionUsageDescription</key>
    <string>HUB uses the gyroscope to automatically rotate the player to fullscreen when you turn your device.</string>

    <!-- ── ATS (dev only — enforce HTTPS in production) ────────────── -->
    <key>NSAppTransportSecurity</key>
    <dict>
        <key>NSAllowsArbitraryLoads</key>
        <true/>
        <!-- In production, replace with:
        <key>NSExceptionDomains</key>
        <dict>
            <key>yourdomain.com</key>
            <dict>
                <key>NSExceptionAllowsInsecureHTTPLoads</key>
                <false/>
                <key>NSExceptionRequiresForwardSecrecy</key>
                <false/>
                <key>NSIncludesSubdomains</key>
                <true/>
            </dict>
        </dict>
        -->
    </dict>

    <!-- ── Deep link URL scheme ─────────────────────────────────────── -->
    <key>CFBundleURLTypes</key>
    <array>
        <dict>
            <key>CFBundleTypeRole</key>
            <string>Editor</string>
            <key>CFBundleURLName</key>
            <string>com.hub.streaming</string>
            <key>CFBundleURLSchemes</key>
            <array>
                <string>hub</string>
            </array>
        </dict>
    </array>

    <!-- ── Status bar ────────────────────────────────────────────────── -->
    <key>UIStatusBarStyle</key>
    <string>UIStatusBarStyleLightContent</string>
    <key>UIViewControllerBasedStatusBarAppearance</key>
    <false/>

    <key>CADisableMinimumFrameDurationOnPhone</key>
    <true/>

    <key>UIApplicationSupportsIndirectInputEvents</key>
    <true/>
</dict>
</plist>
